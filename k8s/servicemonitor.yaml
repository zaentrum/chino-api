apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: chino-api
  labels:
    app: chino-api
    prometheus: platform
spec:
  selector:
    matchLabels:
      app: chino-api
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
